# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/qrmenulerim"
# Generate a long random value before production use
AUTH_SECRET="replace-with-a-long-random-secret"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Only needed when the database seed command is intentionally run
SEED_ADMIN_PASSWORD="replace-with-a-strong-admin-password"
SEED_DEMO_PASSWORD="replace-with-a-strong-demo-password"
